server:
  port: 8001


#指定注册中心地址
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/

#服务的名称
spring:
  application:
    name: product-service